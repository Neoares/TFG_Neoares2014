var WebSocket = require('ws');
var ws = new WebSocket('ws://alocalhost:8888');
ws.on('open', function() {
    ws.send('something');
});
ws.on('message', function(data, flags) {
    // flags.binary will be set if a binary data is received
    // flags.masked will be set if the data was masked
});